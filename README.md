# GP_Consistency_Importance
We will use Gaussian Process Posteriors that model a team's overall rating(EPA, Success rate) to determine if not only high performance in those windows predict game outcome, but consistent high performance.
